const Loading = () => {
  return (
    <div className="w-full h-[50vh] flex items-center justify-center">
      <img className="w-10 animate-ping" src="/logo.png" alt="loader spinner" />
    </div>
  );
};

export default Loading;
